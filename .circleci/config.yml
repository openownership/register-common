version: 2.1
jobs:
  build:
    machine:
      image: ubuntu-2204:current
      docker_layer_caching: true
    steps:
      - checkout
      - run:
          name: Test
          command: |
            cp .env.example .env
            docker compose run common test
